<!DOCTYPE html>
<html lang="en">
<%- include('includes/head') %>
<body>
    <script src="/js/profile-photo-upload.js"></script>
    
    <div class="page-container">
    <div class="bg-gradient"></div>
        <nav class="navbar">
        <div class="navbar-content">
            <h1 style="margin-top: 15px;" class="title-main">HOME</h1>
            <div class="nav-buttons">
            <% if (user) { %>
                <button id="search-toggle" class="btn btn-secondary" type="button">
                    <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                        <circle cx="11" cy="11" r="8"></circle>
                        <path d="m21 21-4.35-4.35"></path>
                    </svg>
                </button>
                <div id="search-container" style="display: none;">
                    <form action="/search-user" method="post" style="display: flex; gap: 0.5rem; align-items: center;">
                        <input type="text" name="username" placeholder="Search username" style="padding: 0.5rem; border: 2px solid rgba(127, 29, 29, 0.5); background: rgba(0, 0, 0, 0.6); color: #e5e7eb; border-radius: 4px;">
                        <button type="submit" class="btn btn-primary" style="padding: 0.5rem 0.75rem;">Search</button>
                    </form>
                </div>
                <a class="btn btn-secondary" href="/logout">
                    <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                        <path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"/><polyline points="16 17 21 12 16 7"/><line x1="21" x2="9" y1="12" y2="12"/>
                    </svg><span>LOG OUT</span>
                </a>

                <% } else { %>
                <a class="btn btn-secondary" href="/login">
                    <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                        <path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"/><polyline points="16 17 21 12 16 7"/><line x1="21" x2="9" y1="12" y2="12"/>
                    </svg><span>LOG IN</span>
                </a>
                <% } %>
            </div>
        </div>
        </nav>
        <main class="main-content">
        <% if (locals.searchError) { %>
        <div style="color: red; text-align: center; margin: 20px 0;"><%= locals.searchError %></div>
        <% } %>
        <% if (posts.length == 0) { %>
        <div class="user-profile-header">
            <div class="nav-buttons" style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 15px;">
                <a class="btn btn-primary" href="/create-post">
                    <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                        <path d="M5 12h14"/><path d="M12 5v14"/>
                    </svg><span>CREATE POST NOW !</span>
                </a>
                <div style="display: flex; align-items: center;">
                    <button id="search-toggle" class="btn btn-secondary" type="button" style="margin-left: 10px;">
                        <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                            <circle cx="11" cy="11" r="8"></circle>
                            <path d="m21 21-4.35-4.35"></path>
                        </svg>
                    </button>
                    <div id="search-container" style="display: none; margin-left: 10px;">
                        <form action="/search-user" method="post" style="display: flex; align-items: center;">
                            <input type="text" name="username" placeholder="Search username" style="padding: 0.5rem; border: 2px solid rgba(127, 29, 29, 0.5); background: rgba(0, 0, 0, 0.6); color: #e5e7eb; border-radius: 4px;" onkeydown="if(event.key === 'Enter') this.form.submit();">
                        </form>
                    </div>
                </div>
            </div>
            <div class="profile-photo-container" id="profile-photo-container" title="Click to upload profile photo">
                <% if (user.profilephoto) { %>
                    <img id="profile-photo-img" src="<%= user.profilephoto %>" alt="Profile" class="profile-photo">
                <% } else { %>
                    <div id="profile-photo-default" class="profile-photo-default">
                        <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
                            <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/>
                            <circle cx="12" cy="7" r="4"/>
                        </svg>
                    </div>
                <% } %>
                <div class="profile-photo-overlay">
                    <div class="profile-photo-upload-text">
                        <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                            <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/>
                            <polyline points="17 8 12 3 7 8"/>
                            <line x1="12" y1="3" x2="12" y2="15"/>
                        </svg>
                        <span>Upload</span>
                    </div>
                </div>
                <input type="file" id="profile-photo-input" accept="image/*" style="display: none;">
            </div>
            <h2 class="page-title">Hi <%= user.username %>, Publish your posts here</h2>
        </div>

        <% } else { %>
        <div class="user-profile-header">
            <div class="nav-buttons" style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 15px;">
                <a class="btn btn-primary" href="/create-post">
                    <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                        <path d="M5 12h14"/><path d="M12 5v14"/>
                    </svg><span>CREATE POST</span>
                </a>
                <div style="display: flex; align-items: center;">
                    <button id="search-toggle" class="btn btn-secondary" type="button" style="margin-left: 10px;">
                        <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                            <circle cx="11" cy="11" r="8"></circle>
                            <path d="m21 21-4.35-4.35"></path>
                        </svg>
                    </button>
                    <div id="search-container" style="display: none; margin-left: 10px;">
                        <form action="/search-user" method="post" style="display: flex; align-items: center;">
                            <input type="text" name="username" placeholder="Search username" style="padding: 0.5rem; border: 2px solid rgba(127, 29, 29, 0.5); background: rgba(0, 0, 0, 0.6); color: #e5e7eb; border-radius: 4px;" onkeydown="if(event.key === 'Enter') this.form.submit();">
                        </form>
                    </div>
                </div>
            </div>
            <div class="profile-photo-container" id="profile-photo-container" title="Click to upload profile photo">
                <% if (user.profilephoto) { %>
                    <img id="profile-photo-img" src="<%= user.profilephoto %>" alt="Profile" class="profile-photo">
                <% } else { %>
                    <div id="profile-photo-default" class="profile-photo-default">
                        <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
                            <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/>
                            <circle cx="12" cy="7" r="4"/>
                        </svg>
                    </div>
                <% } %>
                <div class="profile-photo-overlay">
                    <div class="profile-photo-upload-text">
                        <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                            <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/>
                            <polyline points="17 8 12 3 7 8"/>
                            <line x1="12" y1="3" x2="12" y2="15"/>
                        </svg>
                        <span>Upload</span>
                    </div>
                </div>
                <input type="file" id="profile-photo-input" accept="image/*" style="display: none;">
            </div>
            <h2 class="page-title"><%= user.username %> Profile</h2>
        </div>
        <% } %>
        <br/>
        <div class="posts-grid">
            <% posts.forEach(post =>{ %>
                    <% if (post.imageurl) { %>
                    <a class="post-card" href="/post/<%= post.id %>">
                    <div class="post-image-container">
                        <img class="post-image" src="<%= post.imageurl %>" alt="thumb"  />
                        <div class="post-image-overlay"></div>
                    </div>
                    <% } %>
                    
                    <div class="post-content">
                        <h3 class="post-title" >
                            <%= post.title %>
                        </h3>
                        <p class="post-date">
                            <%= new Date(post.createdate).getMonth()+1 %>-<%= new Date(post.createdate).getDate() %>-<%= new Date(post.createdate).getFullYear()%>
                        </p>
                    </div>
                    </p>
                    </a>
            <% }) %>
        </div>
        </main>
    </div>
    
    <%- include('includes/footer') %>
</body>
<script>
document.getElementById('search-toggle').addEventListener('click', function() {
    const container = document.getElementById('search-container');
    container.style.display = container.style.display === 'none' ? 'block' : 'none';
});
</script>
